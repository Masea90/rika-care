services:
  - type: web
    name: rika-care
    env: node
    buildCommand: cd backend && npm install
    startCommand: ./start.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true

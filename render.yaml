services:
  - type: web
    name: rika-care
    env: node
    buildCommand: npm install --prefix backend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: CACHE_BUST
        value: "2025-12-13-v2"
